//  새 배열을 만듬
split();
a.toLowerCase();
slice();
fruit.concat();
reduce();

//  오리지날 배열을 바꿈
reverse();
splice();
push();

// string으로 반환
const multiply2 = (num) => {
  return [...(num + "")];
};
console.log(multiply2(26));

join();

// map : 새 배열을 만듬 ******
